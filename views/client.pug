doctype html
html
  head
    title= board.title + " | party"
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    link(rel="stylesheet" href="/css/src.bundle.css")
    script(src="https://cdn.socket.io/socket.io-1.4.5.js")
  body(data-slug= board._id)
    #root
    script(src="/js/commons.js")
    script.
      var yogaTitle = !{JSON.stringify(board.title).replace(/<\//g, "<\\/")}
      var yogaFeats = !{JSON.stringify(board.features).replace(/<\//g, "<\\/")}
    script(src="/js/client.bundle.js")
